#n.pop(index) will remove the item at index from the list and return it to you it start with 0 1 2 3 4 5
#n.remove(item) will remove the actual item if it finds it it start with 1 2 3 4
#del(n[1]) is like .pop in that it will remove the item at the given index, but it won’t return, it start with 0 1 2 3 4 5
n = [1, 3, 5]
n.pop(0)
print(n)