# Print out the types of an integer, a float,
# and a string on separate lines below.
#type() function returns the type of the data it receives as an argument.
#NOTE - it use only --- type not typeOf 

print(type(12))
print(type(12.3))
print(type("vim"))